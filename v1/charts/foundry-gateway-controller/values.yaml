# Default values for foundry-gateway-controller.

replicaCount: 1

image:
  repository: containers.catalystsquad.com/public/catalystcommunity/foundry
  # Defaults to the chart appVersion when empty.
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []

nameOverride: ""
fullnameOverride: ""

# Controller behavior. These map directly to the `foundry gateway controller`
# flags. The defaults match a Foundry-installed Contour.
controller:
  gatewayName: contour
  gatewayNamespace: projectcontour
  envoyService: contour-envoy
  # Set to "" to skip NetworkPolicy reconciliation.
  networkPolicy: contour-envoy
  # Resync interval for the watch loop (Go duration).
  interval: 15s
  # Extra raw args appended to the controller command.
  extraArgs: []

serviceAccount:
  create: true
  name: ""
  annotations: {}

rbac:
  # Create the ClusterRole and ClusterRoleBinding the controller needs.
  create: true

# Pod-level security context. The image is distroless:nonroot, whose user is the
# symbolic name "nonroot" — Kubernetes can't verify a non-numeric user is
# non-root, so we pin the numeric uid/gid (65532 = distroless nonroot).
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi

nodeSelector: {}
tolerations: []
affinity: {}
podAnnotations: {}
podLabels: {}
