; Component configuration types for Foundry
;
; Defines flexible component configuration with support for arbitrary settings.

; Map of component names to their configurations
ComponentMap = {* text => ComponentConfig}

; Individual component configuration
; Supports arbitrary additional configuration via inline map
ComponentConfig = {
    ? version: text,
    ? hosts: [* text],
    ; Additional config stored as flexible map (yaml:",inline")
    * text => any
}
