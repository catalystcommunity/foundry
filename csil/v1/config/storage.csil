; Storage configuration types for Foundry
;
; Defines storage backend settings, currently supporting TrueNAS.

; Storage backend configuration
StorageConfig = {
    ? truenas: TrueNASConfig
}

; TrueNAS-specific settings
TrueNASConfig = {
    api_url: text,
    api_key: text
}
